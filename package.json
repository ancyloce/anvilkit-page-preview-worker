{
  "name": "anvilkit-page-preview-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isolated deterministic renderer for AnvilKit page candidates.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "node --test 'test/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
